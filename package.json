{
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../backend && npm install",
    "start:frontend": "cd client && npm run serve",
    "start:backend": "cd backend && npm run server",
    "install:packages": "npm install && cd client && npm install && cd ../backend && npm install",
    "start:both": "npm-run-all -p start:frontend start:backend",
    "start": "npm run install:packages && npm run start:backend"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
